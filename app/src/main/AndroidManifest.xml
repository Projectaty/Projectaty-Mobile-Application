<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projectaty"
        tools:targetApi="31">
        <activity
            android:name=".activities.projectmanagment.ProjectDashboard"
            android:exported="false" />
        <activity
            android:name=".activities.taskmanagement.SearchTask"
            android:exported="true" />
        <activity
            android:name=".activities.taskmanagement.UpdateDelTask"
            android:exported="true" />
        <activity
            android:name=".activities.taskmanagement.CreateTask"
            android:exported="true" />
        <activity
            android:name=".activities.taskmanagement.TaskList"
            android:exported="true" />
        <activity
            android:name=".activities.projectmanagment.CreateProject"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>